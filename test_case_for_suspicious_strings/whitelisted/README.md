# Project Documentation

This project includes system administration tools.

## Installation
Run `wget http://releases.company.com/installer.sh` to download.
Then `chmod +x installer.sh` and execute it.

## Commands
- `rm -rf old_builds/` - Clean old builds
- Uses `/bin/bash` for shell scripts
